<nav class="navbar navbar-expand-lg bg-body-tertiary">
	<div class="container-fluid">
		<a class="navbar-brand" href="<%= root_path %>">Inventarios</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
			aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="<%= vehicles_path %>">Vehículos</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="<%= dealerships_path %>">Consecionarias</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="<%= sales_path %>">Ventas</a>
				</li>
				<% if user_signed_in? %>
					<li class="nav-item">
						<%= button_to 'Cerrar sesión' , destroy_user_session_path, method: :delete, class:"nav-item btn btn-danger" %>
					</li>
					<% end %>
			</ul>
		</div>
	</div>
</nav>